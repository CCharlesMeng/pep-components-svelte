# PEP Spec - 需求规格澄清

## 🎯 职责

通过对话式交互，澄清和完善组件的需求规格（spec.md）。

**核心目标**：
1. 识别 spec.md 中不清晰、缺失、模糊的部分
2. 通过智能对话收集完整需求
3. 增量更新 spec.md
4. 确保需求清晰、全面、无歧义

**下一步**：运行 `/pep-plan` 生成开发任务

---

## 📋 执行流程

### 阶段 0: 前置检查

```markdown
1. 检查组件是否存在
   components/<component-name>/ 不存在
   → 提示：请先运行 /pep-start 创建组件

2. 检查 spec.md 是否存在
   spec.md 不存在
   → 提示：文件缺失，请先运行 /pep-start

3. 读取当前 spec.md 内容
   → 进入【阶段 1：分析 spec 质量】
```

---

### 阶段 1: 分析 spec.md 质量

#### Agent 行为指令

```markdown
读取 spec.md，分析以下维度：

1. 概述是否清晰？
   - 只有占位符 → 缺失
   - 一句话简述 → 基本
   - 详细说明 → 完整

2. 核心功能是否具体？
   - 空或模板内容 → 缺失
   - 功能列表但无细节 → 不完整
   - 详细描述 + 子功能点 → 完整

3. Props 设计是否明确？
   - 空或只有示例 → 缺失
   - 有列表但无说明 → 不完整
   - 完整表格 + 类型 + 默认值 → 完整

4. 验收标准是否具体？
   - 空或通用模板 → 缺失
   - 有清单但不具体 → 不完整
   - 详细可测试的标准 → 完整

5. **视觉设计规范是否明确？（关键）**
   - 无样式描述 → 缺失
   - 仅简单描述（如"卡片布局"） → 不完整
   - 详细设计规范（布局、颜色、间距、字体、图标、动画） → 完整
   - **有设计稿/参考图** → 优先级最高，进入【阶段 1.5：视觉设计分析】

6. 特殊需求章节
   - 事件设计、无障碍、性能要求等

基于分析结果，判断 spec 质量：
- 高质量（80%+ 完整）→ 进入【阶段 2A：微调模式】
- 中等质量（50-80%）→ 进入【阶段 2B：补充模式】
- 低质量（< 50%）→ 进入【阶段 2C：重构模式】
- **有设计稿/参考图** → 优先进入【阶段 1.5：视觉设计分析】
```

---

### 阶段 1.5: 视觉设计分析（有设计稿/参考图时）

#### Agent 行为指令

```markdown
当用户提供设计稿、截图或参考图时，必须进入此阶段进行详细的视觉设计分析。

目标：原封不动地复刻设计稿的所有视觉细节。
```

#### 分析维度（按优先级）

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎨 设计稿分析中...

已识别设计要素：

【1. 布局结构】
分析问题：
• 整体布局方式：网格 / 弹性 / 固定？
• 列数和响应式断点：桌面几列？移动端几列？
• 元素排列：水平 / 垂直 / 交错？
• 内容对齐：左对齐 / 居中 / 两端对齐？

示例识别（基于华为云截图）：
✓ 2x2 网格布局（桌面）
✓ 移动端推测：1列堆叠
✓ 卡片等高，固定间距

【2. 间距系统】
分析问题：
• 组件间距：卡片之间的间隔？
• 内边距：卡片内部的 padding？
• 外边距：整体容器的 margin？
• 行列间距：grid-gap / flex-gap 值？

示例识别：
✓ 卡片间距：约 24px
✓ 卡片内边距：约 32px
✓ 元素垂直间距：标题到描述 12px

【3. 颜色系统】
分析问题：
• 主色调：品牌色、强调色？
• 背景色：整体背景、卡片背景、图标背景？
• 文字颜色：标题、正文、辅助文字的色值？
• 边框颜色：是否有描边？颜色和粗细？

示例识别（基于截图）：
✓ 主标题：深灰色 #1A1A1A
✓ 描述文字：中灰色 #666666
✓ 卡片背景：浅灰色 #F5F7FA
✓ 插图主色：蓝色系 #007AFF ~ #00D4FF 渐变
✓ 插图辅色：绿色 #00E5A0

【4. 字体排印】
分析问题：
• 字体家族：系统字体 / 自定义字体？
• 字号层级：大标题 / 小标题 / 正文 / 辅助文字？
• 字重：粗体 / 常规 / 细体？
• 行高：标题和正文的 line-height？

示例识别：
✓ 大标题（页面标题）：32px / Bold / 1.4行高
✓ 卡片标题：20px / SemiBold / 1.5行高
✓ 描述文字：14px / Regular / 1.8行高
✓ 字体：系统默认（思源黑体 / Helvetica）

【5. 图标/插图】
分析问题：
• 插图类型：矢量图 / 位图 / 图标字体？
• 插图风格：扁平 / 立体 / 渐变 / 线性？
• 插图尺寸：宽高比例？
• 插图位置：左侧 / 右侧 / 顶部 / 背景？

示例识别：
✓ 插图风格：扁平风格 + 渐变元素
✓ 插图尺寸：约 200x160px
✓ 插图位置：卡片右侧 / 底部
✓ 配色：蓝绿渐变系

【6. 卡片样式】
分析问题：
• 圆角：border-radius 值？
• 阴影：box-shadow 参数？
• 背景：纯色 / 渐变 / 图片？
• 边框：是否有边框？粗细和颜色？

示例识别：
✓ 圆角：8px
✓ 阴影：无明显阴影（或极浅）
✓ 背景：纯色浅灰 #F5F7FA
✓ 边框：无

【7. 响应式设计】
分析问题：
• 桌面端：几列？最小宽度？
• 平板端：几列？断点在哪？
• 移动端：几列？是否堆叠？
• 图片适配：等比缩放 / 裁剪？

示例推断：
✓ 桌面（> 1024px）：2x2 网格
✓ 平板（768-1024px）：2列
✓ 移动（< 768px）：1列堆叠
✓ 插图在移动端：缩小或隐藏

【8. 交互状态】
分析问题：
• Hover 状态：卡片悬停效果？
• Active 状态：点击时的反馈？
• Focus 状态：键盘聚焦样式？
• 动画效果：过渡动画？时长和缓动？

示例推断：
✓ Hover：卡片轻微上浮 + 阴影加深
✓ 过渡：transform 0.3s ease-out
✓ 无明显点击反馈（纯展示型）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

基于以上分析，我将：

A. 直接生成完整的【视觉设计规范】章节写入 spec.md
B. 询问几个关键细节（如精确颜色值、动画参数）
C. 用户提供补充说明

请回复 A / B / C，或直接补充说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### spec.md 中的【视觉设计规范】章节模板

**Agent 必须生成此章节并写入 spec.md**：

```markdown
## 视觉设计规范

### 布局结构
- **整体布局**: 2x2 网格布局（Grid）
- **桌面端**: 2 列，每行 2 个卡片
- **移动端**: 1 列，垂直堆叠
- **断点**: 
  - Desktop: `min-width: 1024px` → 2 列
  - Tablet: `768px - 1024px` → 2 列（间距缩小）
  - Mobile: `< 768px` → 1 列

### 间距系统
- **卡片间距**: 24px (grid-gap: 24px)
- **卡片内边距**: 32px
- **容器外边距**: 60px 0 (顶部和底部)
- **标题到描述**: 12px
- **描述到插图**: 24px

### 颜色系统
```css
/* 背景色 */
--bg-page: #FFFFFF;
--bg-card: #F5F7FA;

/* 文字色 */
--text-title: #1A1A1A;      /* 标题 */
--text-body: #666666;        /* 正文 */
--text-label: #999999;       /* 辅助文字 */

/* 插图主色 */
--color-primary: #007AFF;    /* 蓝色 */
--color-accent: #00E5A0;     /* 绿色 */
--color-gradient-start: #007AFF;
--color-gradient-end: #00D4FF;
```

### 字体排印
- **页面标题**: 
  - 字号: 32px
  - 字重: 700 (Bold)
  - 行高: 1.4
  - 颜色: var(--text-title)
  
- **卡片标题**:
  - 字号: 20px
  - 字重: 600 (SemiBold)
  - 行高: 1.5
  - 颜色: var(--text-title)
  
- **描述文字**:
  - 字号: 14px
  - 字重: 400 (Regular)
  - 行高: 1.8
  - 颜色: var(--text-body)

### 卡片样式
- **圆角**: 8px (border-radius)
- **阴影**: 无（或极浅: `0 2px 8px rgba(0,0,0,0.04)`）
- **背景**: var(--bg-card)
- **边框**: 无
- **最小高度**: 200px

### 插图规范
- **类型**: SVG 矢量插图
- **风格**: 扁平风格 + 渐变元素
- **尺寸**: 200x160px
- **位置**: 卡片右侧或底部
- **配色**: 蓝绿渐变系（使用 --color-gradient-*）
- **移动端**: 缩小至 120x96px

### 交互状态
- **Hover 效果**:
  ```css
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.08);
  transition: all 0.3s ease-out;
  ```
  
- **无 Active 状态**（纯展示组件）

### 响应式断点
```css
/* 桌面 */
@media (min-width: 1024px) {
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
}

/* 平板 */
@media (min-width: 768px) and (max-width: 1023px) {
  grid-template-columns: repeat(2, 1fr);
  gap: 16px;
  padding: 0 24px;
}

/* 移动 */
@media (max-width: 767px) {
  grid-template-columns: 1fr;
  gap: 16px;
  padding: 0 16px;
}
```

### 设计还原要求
- ✅ **精确匹配**: 所有颜色值、字号、间距必须与设计稿完全一致
- ✅ **像素级对齐**: 使用浏览器开发工具对比设计稿
- ✅ **视觉走查**: 完成后与设计稿并排比较，确保无差异
- ✅ **多设备验证**: 在 Desktop / Tablet / Mobile 实际测试

### 参考设计稿
- 设计稿来源: [华为云 MaaS 服务页面]
- 设计风格: 现代扁平风，科技感
- 视觉层级: 清晰的标题、描述、插图三层结构
```

#### Agent 行为指令

```markdown
1. 仔细观察用户提供的设计稿/截图
2. 按照上述 8 个维度逐一分析
3. 生成完整的【视觉设计规范】章节
4. 使用 search_replace 或 write 工具写入 spec.md
5. 如有不确定的细节（如精确色值），询问用户
6. 完成后进入【阶段 3：展示更新预览】
```

---

### 阶段 2A: 微调模式（spec 质量高）

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ spec.md 分析完成

需求规格质量：高

已识别内容：
• 核心功能：3 个模块，描述清晰
• Props 设计：5 个 Props，类型完整
• 验收标准：功能、UI、性能均有覆盖

需求已基本明确，是否需要补充以下细节？

A. 视觉设计规范（颜色、字体、间距、布局细节）
B. 无障碍要求（键盘导航、ARIA 标签）
C. 动画效果细节（过渡动画、缓动函数）
D. 边界情况处理（空数据、异常状态）
E. 不需要，直接使用当前 spec

💡 提示：如果有设计稿/参考图，请上传后选择 A

请回复
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Agent 行为指令**：
- 如果用户选择 A **且提供设计稿** → 进入【阶段 1.5：视觉设计分析】
- 如果用户选择 A **但无设计稿** → 询问设计细节（颜色、字体等）
- 如果用户选择 B/C/D → 针对性询问 1-2 个问题，更新 spec
- 如果用户选择 E → 直接进入【阶段 4：完成提示】
- 如果用户直接上传设计稿 → 立即进入【阶段 1.5：视觉设计分析】

---

### 阶段 2B: 补充模式（spec 质量中等）

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 spec.md 需要补充信息

需求规格质量：中等

我注意到以下章节需要完善：
1. 核心功能描述过于简单
2. Props 设计缺少部分字段说明
3. **视觉设计规范缺失或不明确** ⚠️
4. 验收标准不够具体

💡 **重要提示**：如果您有设计稿/参考图/截图，请现在上传！
   这将帮助我生成精确的视觉设计规范，确保开发产物完美还原设计。

是否有设计稿？
A. 有，我现在上传 → 进入视觉设计分析
B. 没有，继续补充其他需求 → 通过对话收集（约 2-4 个问题）

请回复

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 对话流程（智能推断 + 确认）

**示例：按钮组件**

```markdown
问题 1/3：按钮变体

基于"可配置按钮"的描述，我推断可能需要这些变体：
• primary - 主要按钮（强调操作）
• secondary - 次要按钮
• text - 文本按钮（弱化样式）

是否准确？需要调整吗？
（可回复：对 / 差不多，但... / 不对，实际需要...）

请回复
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 用户回复后

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 已记录：variant 支持 primary / secondary / text

问题 2/3：尺寸选项

推断可能需要的尺寸：
• small - 小号（用于紧凑布局）
• medium - 中号（默认）
• large - 大号（强调按钮）

是否准确？

请回复
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 收集完信息后，进入【阶段 3：展示更新预览】
```

**Agent 行为指令**：
- 每个问题先展示推断结果
- 用户只需确认或纠正
- 收集 2-4 轮信息后，进入【阶段 3】
- 如果用户中途回复 'skip' / '够了' → 保存进度，进入【阶段 3】

---

### 阶段 2C: 重构模式（spec 质量低）

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  spec.md 信息严重不足

需求规格质量：低

当前 spec 内容过于简单，无法准确拆解开发任务。

建议两个选项：

A. 返回 /pep-start 重新生成（使用 AI 模式）
   → 适合：不确定具体需求，希望从头梳理

B. 继续对话补充（可能需要 5-8 个问题）
   → 适合：明确知道要做什么，只是没写清楚

请选择
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**如果用户选择 B**，进入深度对话：

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
好的，让我们从头梳理需求

问题 1/6：组件类型

这个组件主要用于什么？

A. 展示内容（如 Banner、Card、List）
B. 用户交互（如 Button、Input、Modal）
C. 布局容器（如 Grid、Flex、Container）
D. 表单相关（如 Form、Field、Validation）
E. 其他（请描述）

请回复
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 继续深入询问...
```

**Agent 行为指令**：
- 按层次询问：类型 → 核心功能 → 交互方式 → 样式需求
- 每次展示推断 + 确认
- 允许用户随时 'skip'（保存进度）
- 收集完后进入【阶段 3】

---

### 阶段 3: 展示更新预览

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 信息收集完成

我将在 spec.md 中补充/更新以下内容：

【核心功能】
• 样式变体支持（primary / secondary / text）
• 尺寸选项（small / medium / large）
• 禁用状态
• 加载状态

【Props 设计】
| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| variant | 'primary' \| 'secondary' \| 'text' | 'primary' | 按钮样式变体 |
| size | 'small' \| 'medium' \| 'large' | 'medium' | 按钮尺寸 |
| disabled | boolean | false | 是否禁用 |
| loading | boolean | false | 是否加载中 |
| onClick | () => void | - | 点击事件回调 |

【验收标准】
• 所有变体样式正确显示
• 所有尺寸在不同设备正常显示
• 禁用状态不可点击，样式变灰
• 加载状态显示 spinner，禁止点击

是否确认更新 spec.md？[Y/n]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Agent 行为指令**：
- 用户确认后，使用 `search_replace` 或 `write` 工具更新 spec.md
- 采用**增量更新**方式（保留用户原有内容）
- 如果章节不存在，追加新章节
- 如果章节存在，合并内容（不覆盖）
- 更新完成后，进入【阶段 4】

---

### 阶段 4: 完成提示

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ spec.md 已更新！

📝 更新内容

已补充/完善以下章节：
• 核心功能（新增 2 个功能点）
• Props 设计（补充 4 个 Props）
• 验收标准（细化测试要点）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 下一步操作

1️⃣  生成开发任务（推荐）
   /pep-plan <component-name>
   
   基于完善的 spec.md 生成 features.json

2️⃣  继续完善 spec（可选）
   如需补充更多细节，可以再次运行：
   /pep-spec <component-name>

3️⃣  直接开始开发（不推荐）
   /pep-next <component-name>
   
   建议先运行 /pep-plan 生成任务列表

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 提示

• spec.md 现在可以作为开发的指导文档
• 随时可以再次运行 /pep-spec 补充需求
• 需求变更时，先更新 spec，再重新运行 /pep-plan

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🎯 关键设计原则

### 1. 智能推断优先
- 基于组件类型和常见模式推断设计
- 展示推断结果，用户只需确认或纠正
- 比逐项询问效率高很多

### 2. 分层对话策略
```
spec 质量 → 对话轮次

高质量（80%+）
  → 0-1 轮（只问需不需要补充细节）

中等质量（50-80%）
  → 2-4 轮（补充关键缺失项）

低质量（< 50%）
  → 建议重新开始，或深度对话 5-8 轮
```

### 3. 灵活退出机制
- 用户可随时回复 'skip' / '够了' / '就这样'
- Agent 立即停止询问，保存已收集信息
- 基于已有信息尽力完善 spec
- 标注推断的部分（如 "<!-- AI 推断，请确认 -->"）

### 4. 增量更新
- 不覆盖用户已有内容
- 只补充缺失部分
- 合并新旧内容
- 保持 spec 结构完整

---

## 📤 更新策略

### spec.md 增量更新规则

```markdown
1. 读取当前 spec.md 全文

2. 解析章节结构
   ## 概述
   ## 核心功能
   ## Props 设计
   ...

3. 对每个需要更新的章节：
   
   【如果章节不存在】
   → 追加新章节到适当位置
   
   【如果章节存在但内容为空/模板】
   → 填充新内容
   
   【如果章节存在且有用户内容】
   → 合并内容：
     • Props 表格：追加新行，不删除现有行
     • 功能列表：追加新项，保留现有项
     • 验收标准：追加新标准，保留现有标准

4. 使用 search_replace 或 write 工具更新文件
```

### 示例：Props 章节合并

**原有内容**：
```markdown
## Props 设计

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| className | string | '' | 自定义类名 |
```

**新增内容**：
```markdown
| variant | 'primary' | 'secondary' | 'primary' | 按钮变体 |
| size | 'small' | 'medium' | 'large' | 'medium' | 按钮尺寸 |
```

**合并结果**：
```markdown
## Props 设计

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| className | string | '' | 自定义类名 |
| variant | 'primary' \| 'secondary' | 'primary' | 按钮变体 |
| size | 'small' \| 'medium' \| 'large' | 'medium' | 按钮尺寸 |
```

---

## 🤔 典型使用场景

### 场景 A：初次完善（紧接 /pep-start）

```
用户：/pep-start pep-button
Agent：✅ 生成完成，下一步运行 /pep-spec

用户：/pep-spec pep-button
Agent：分析 spec.md（内容为模板）
     → 进入补充模式
     → 通过 3 个问题收集需求
     → 更新 spec.md
     → 提示运行 /pep-plan
```

### 场景 B：中途补充需求

```
开发中发现需要新功能

用户：/pep-spec pep-button
Agent：分析 spec.md（已有内容）
     → spec 质量高
     → 询问："需要补充什么内容？"

用户：需要增加 danger 变体
Agent：理解需求
     → 更新 Props 设计章节（增量）
     → 提示建议重新运行 /pep-plan
```

### 场景 C：澄清模糊需求

```
spec 中有模糊描述

用户：/pep-spec pep-form
Agent：读取 spec
     → 发现"表单验证"描述不清楚

用户：帮我明确一下表单验证的部分
Agent：针对性询问验证相关细节
     → 更新 spec.md 对应章节
```

---

## ⚠️  注意事项

1. **不要过度询问**
   - 优先使用智能推断
   - 最多 1-2 轮确认
   - 低质量 spec 才深度对话

2. **保存用户进度**
   - 支持中途退出
   - 已收集的信息不丢失
   - 允许分多次完善

3. **增量更新**
   - 永远不要删除用户已有内容
   - 只追加或合并
   - 冲突时提示用户选择

4. **明确引导**
   - 完成后告诉用户下一步
   - 推荐运行 /pep-plan
   - 说明 spec 的作用

---

## 🎯 成功标准

- ✅ spec.md 内容清晰、全面、无歧义
- ✅ 所有关键章节已填写（概述、核心功能、Props、验收标准）
- ✅ **视觉设计规范完整**（有设计稿时必须包含详细的样式规范）
- ✅ 设计规范可执行（包含精确的颜色值、字号、间距、布局参数）
- ✅ 用户原有内容被保留
- ✅ 新内容与原有内容正确合并
- ✅ 用户清楚下一步操作（运行 /pep-plan）
- ✅ spec 可以作为 /pep-plan 的输入

### 视觉设计规范的完整性要求

当用户提供设计稿/参考图时，spec.md **必须包含**以下内容：

- ✅ 布局结构（Grid/Flex 参数、列数、断点）
- ✅ 间距系统（padding、margin、gap 的具体数值）
- ✅ 颜色系统（所有颜色的精确色值，使用 CSS 变量）
- ✅ 字体排印（字号、字重、行高、字体家族）
- ✅ 卡片/组件样式（圆角、阴影、边框参数）
- ✅ 插图/图标规范（尺寸、位置、风格描述）
- ✅ 交互状态（Hover/Active/Focus 的视觉反馈）
- ✅ 响应式设计（各断点的布局变化）
- ✅ 设计还原要求（精确匹配、像素级对齐）

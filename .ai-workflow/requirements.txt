# Cursor 自主编码流程依赖
# ============================

# ✅ 核心功能：无外部依赖
# 所有脚本仅使用 Python 3.8+ 标准库

# 如果你需要增强功能（可选），可以取消注释以下依赖：

# rich>=13.0.0         # 更美观的终端输出（可选）
# gitpython>=3.1.0     # Git 操作增强（可选，目前使用 subprocess）
# jsonschema>=4.0.0    # JSON schema 验证（可选，目前使用手动验证）
# click>=8.0.0         # 更强大的 CLI 工具（可选）

# 注意：
# - 默认情况下不需要安装任何依赖即可使用
# - Python 3.8+ 标准库已经足够
# - 可选依赖仅用于增强体验，非必需

import defaultData from './default.json';
import promotionData from './promotion.json';

/**
 * Mock 数据出口
 * 
 * 💡 提示：可以在此处手动切换导出的数据对象，
 * 也可以根据需要组合或动态生成数据。
 */
export default defaultData;

// 如果需要调试促销场景，请取消下面一行的注释并注释掉上面的导出
// export default promotionData;

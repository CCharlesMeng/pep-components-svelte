import sharedConfig from '@pep/config/svelte.config.js';

/** @type {import('@sveltejs/kit').Config} */
const config = {
	...sharedConfig,
	// Add component-specific config here if needed
};

export default config;
